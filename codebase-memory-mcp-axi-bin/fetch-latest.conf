pkg_type=github
repo=nikolauska/codebase-memory-mcp-axi
asset_regex='^cbm-axi-linux-amd64$'
strip_prefix=v
# Expanded by fetch-latest-release.sh after pkgver is known.
# shellcheck disable=SC2016
dest_name_template='cbm-axi-${pkgver}-${CARCH}'
