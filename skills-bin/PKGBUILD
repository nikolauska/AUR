# Maintainer: Niko Lehtovirta <niko@lehtovirta.dev>

pkgname=skills-bin
_pkgname=skills
pkgver=1.5.16
pkgrel=1
pkgdesc="CLI for the open agent skills ecosystem"
arch=('any')
url="https://github.com/vercel-labs/skills"
license=('MIT')
depends=('git' 'nodejs>=18')
makedepends=('npm')
provides=('skills')
conflicts=('skills')

source=("https://registry.npmjs.org/${_pkgname}/-/${_pkgname}-${pkgver}.tgz")
noextract=("${_pkgname}-${pkgver}.tgz")
sha512sums=('3bea6372b9e6e569124810f7589c63743b2c13ea329e6ee4d4b9ca3235efb157907662a663f6be822e9650e872195218a6d905981044868ff3c9fdb652d37a73')

package() {
  npm install -s -g \
    --cache "${srcdir}/npm-cache" \
    --prefix "${pkgdir}/usr" \
    "${srcdir}/${_pkgname}-${pkgver}.tgz"

  install -Dm644 \
    "${pkgdir}/usr/lib/node_modules/${_pkgname}/ThirdPartyNoticeText.txt" \
    "${pkgdir}/usr/share/licenses/${pkgname}/ThirdPartyNoticeText.txt"
}
