# Maintainer: Niko Lehtovirta <niko@lehtovirta.dev>

pkgname=quota-axi-bin
_pkgname=quota-axi
pkgver=0.1.45
pkgrel=1
pkgdesc="AXI CLI that reports local agent-provider quota windows"
arch=('any')
url="https://github.com/kunchenguid/quota-axi"
license=('MIT')
depends=('nodejs>=22.19')
makedepends=('npm')
provides=('quota-axi')
conflicts=('quota-axi')

source=("https://registry.npmjs.org/${_pkgname}/-/${_pkgname}-${pkgver}.tgz")
noextract=("${_pkgname}-${pkgver}.tgz")
sha512sums=('43ba26255a208353ac6f2a2a00d168d8ac06f1827e9ed9b3611c49712cbcb7dcfdc37f134b6432ede71669aa5f368b6074fc00134f74ab66aff0d3914a442e0a')

package() {
  npm install -s -g \
    --cache "${srcdir}/npm-cache" \
    --prefix "${pkgdir}/usr" \
    "${srcdir}/${_pkgname}-${pkgver}.tgz"

  install -Dm644 \
    "${pkgdir}/usr/lib/node_modules/${_pkgname}/LICENSE" \
    "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
