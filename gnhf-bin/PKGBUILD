# Maintainer: Niko Lehtovirta <niko@lehtovirta.dev>

pkgname=gnhf-bin
_pkgname=gnhf
pkgver=0.1.48
pkgrel=1
pkgdesc="Keep coding agents running in an iterative loop"
arch=('any')
url="https://github.com/kunchenguid/gnhf"
license=('MIT')
depends=('git' 'nodejs>=20')
makedepends=('npm')
provides=('gnhf')
conflicts=('gnhf')

source=("https://registry.npmjs.org/${_pkgname}/-/${_pkgname}-${pkgver}.tgz")
noextract=("${_pkgname}-${pkgver}.tgz")
sha512sums=('5748d615723bcf58a9c22a88ad5f76e6a20005d465757e59867d98ede57630089334ad16d803c42616569075f922476125c01c5d05306152d36c78b6db4309e3')

package() {
  npm install -s -g \
    --cache "${srcdir}/npm-cache" \
    --prefix "${pkgdir}/usr" \
    "${srcdir}/${_pkgname}-${pkgver}.tgz"

  install -Dm644 \
    "${pkgdir}/usr/lib/node_modules/${_pkgname}/LICENSE" \
    "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
