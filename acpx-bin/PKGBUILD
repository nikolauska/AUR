# Maintainer: Niko Lehtovirta <niko@lehtovirta.dev>

pkgname=acpx-bin
_pkgname=acpx
pkgver=0.17.0
pkgrel=1
pkgdesc="Headless CLI client for stateful Agent Client Protocol sessions"
arch=('x86_64' 'aarch64')
options=('!debug' '!strip')
url="https://github.com/openclaw/acpx"
license=('MIT')
depends=('nodejs>=22.13.0')
makedepends=('npm')
provides=('acpx')
conflicts=('acpx')

source=("https://registry.npmjs.org/${_pkgname}/-/${_pkgname}-${pkgver}.tgz")
noextract=("${_pkgname}-${pkgver}.tgz")
sha512sums=('01c83df20bce1fedcf66c16f643e66ff4b84d1ac8d45061c232fe76784b4c8affd3af8f0fa32887d887757b727cf54a10b108164adc486424d0ff7def488d15c')

package() {
  npm install -s -g \
    --cache "${srcdir}/npm-cache" \
    --prefix "${pkgdir}/usr" \
    "${srcdir}/${_pkgname}-${pkgver}.tgz"

  install -Dm644 \
    "${pkgdir}/usr/lib/node_modules/${_pkgname}/LICENSE" \
    "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
