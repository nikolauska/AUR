# Maintainer: Niko Lehtovirta <niko@lehtovirta.dev>

pkgname=gh-axi-bin
_pkgname=gh-axi
pkgver=0.1.29
pkgrel=1
pkgdesc="AXI-compliant GitHub CLI wrapper"
arch=('any')
url="https://github.com/kunchenguid/gh-axi"
license=('MIT')
depends=('github-cli' 'nodejs')
makedepends=('npm')
provides=('gh-axi')
conflicts=('gh-axi')

source=("https://registry.npmjs.org/${_pkgname}/-/${_pkgname}-${pkgver}.tgz")
noextract=("${_pkgname}-${pkgver}.tgz")
sha512sums=('e862ad82ce0bd8c73283085bf78e3d5c494c5d4155b19a2c5fd07abbc35351dd851c225da65de653ed1fa7f5054e83585f2a0c59d281b1293b5568ad14b4aeb2')

package() {
  npm install -s -g \
    --cache "${srcdir}/npm-cache" \
    --prefix "${pkgdir}/usr" \
    "${srcdir}/${_pkgname}-${pkgver}.tgz"

  install -Dm644 \
    "${pkgdir}/usr/lib/node_modules/${_pkgname}/LICENSE" \
    "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
